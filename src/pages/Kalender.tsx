import AppLayout from "@/components/AppLayout";

const Kalender = () => (
  <AppLayout title="Kalender" subtitle="Alle lustrumactiviteiten op een rij">
    <div className="glass-card rounded-xl p-8 text-center">
      <p className="text-muted-foreground">Interactieve kalender komt hier — met RSVP, export en countdowns.</p>
    </div>
  </AppLayout>
);

export default Kalender;
